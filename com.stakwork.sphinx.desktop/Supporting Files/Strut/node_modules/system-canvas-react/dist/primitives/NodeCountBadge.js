import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
/**
 * Count / notification badge.
 *
 * - In `topRightOuter`: a small notched *tab* that hangs off the top-right
 *   corner, clipping slightly into the node's stroke. Matches the
 *   notification-badge pattern.
 * - Anywhere else: a centered rounded pill.
 */
export function NodeCountBadge({ region, value, theme, color, textColor, position, }) {
    const label = typeof value === 'number' ? String(value) : value;
    if (label.length === 0)
        return null;
    const fill = color ?? theme.node.refIndicator.color;
    const text = textColor ?? theme.background;
    if (position === 'topRightOuter') {
        return (_jsx(TabBadge, { region: region, label: label, fill: fill, text: text, theme: theme }));
    }
    return _jsx(PillBadge, { region: region, label: label, fill: fill, text: text, theme: theme });
}
function TabBadge({ region, label, fill, text, theme, }) {
    // Size the tab to the region (a ~1.4em square). Single-digit values fit
    // the square; multi-character values allow the tab to widen leftward.
    const h = region.height;
    const minW = h;
    const fontSize = Math.max(9, h * 0.58);
    const estW = label.length <= 1 ? minW : Math.max(minW, fontSize * 0.7 * label.length + 10);
    const w = Math.min(estW, h * 3);
    // Pin to the region's right edge; extend leftward if it widened.
    const x = region.x + region.width - w;
    const y = region.y;
    const rx = Math.min(6, h / 3);
    const cx = x + w / 2;
    const cy = y + h / 2;
    // Ring the badge in the canvas background color so it reads as a
    // separate element when it sits directly over a same-colored top strip.
    // Stroke is drawn with a slightly larger rect behind the fill so the
    // visible corner radius stays crisp.
    const ringWidth = 2;
    return (_jsxs("g", { pointerEvents: "none", children: [_jsx("rect", { x: x - ringWidth, y: y - ringWidth, width: w + ringWidth * 2, height: h + ringWidth * 2, rx: rx + ringWidth, fill: theme.background }), _jsx("rect", { x: x, y: y, width: w, height: h, rx: rx, fill: fill }), _jsx("text", { x: cx, y: cy + fontSize * 0.35, fill: text, fontSize: fontSize, fontWeight: 700, fontFamily: theme.node.fontFamily, textAnchor: "middle", children: label })] }));
}
function PillBadge({ region, label, fill, text, theme, }) {
    const fontSize = Math.max(9, Math.min(region.height * 0.7, 14));
    const estWidth = Math.max(region.height, region.height * 0.65 + label.length * fontSize * 0.55);
    const width = Math.min(estWidth, region.width * 1.6);
    const height = region.height;
    const cx = region.x + region.width / 2;
    const cy = region.y + region.height / 2;
    const x = cx - width / 2;
    const y = cy - height / 2;
    return (_jsxs("g", { pointerEvents: "none", children: [_jsx("rect", { x: x, y: y, width: width, height: height, rx: height / 2, fill: fill }), _jsx("text", { x: cx, y: cy + fontSize * 0.35, fill: text, fontSize: fontSize, fontWeight: 600, fontFamily: theme.node.fontFamily, textAnchor: "middle", children: label })] }));
}
//# sourceMappingURL=NodeCountBadge.js.map