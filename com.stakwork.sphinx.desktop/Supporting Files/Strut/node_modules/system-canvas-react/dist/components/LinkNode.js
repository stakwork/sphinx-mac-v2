import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { RefIndicator } from './RefIndicator.js';
import { CategorySlotsLayer } from './CategorySlotsLayer.js';
import { NodeText } from '../primitives/NodeText.js';
import { toKebabCorner } from './refCorner.js';
export function LinkNode({ node, theme, onClick, onDoubleClick, onContextMenu, onNavigate, onPointerDown, isSelected, isEditing, slots, canvases, reservedTop = 0, reservedBottom = 0, reservedLeft = 0, reservedRight = 0, refCorner = 'bottomRight', }) {
    const { x, y, width, height } = node;
    const contentX = x + reservedLeft;
    const contentY = y + reservedTop;
    const contentWidth = Math.max(0, width - reservedLeft - reservedRight);
    const contentHeight = Math.max(0, height - reservedTop - reservedBottom);
    // Reserve space at the left for the link glyph so the wrapped URL text
    // doesn't overlap it. Matches the glyph's `contentX + 12` placement plus
    // a small gap.
    const glyphReserve = 20;
    // Comfort padding inside the content rect — keeps the URL from crowding
    // the right border and the top/bottom strokes.
    const LABEL_PAD_X = 10;
    const LABEL_PAD_Y = 6;
    // Parse URL to show hostname
    let displayUrl = node.url ?? '';
    try {
        const url = new URL(displayUrl);
        displayUrl = url.hostname;
    }
    catch {
        // Keep full URL if parsing fails
    }
    return (_jsxs("g", { className: "system-canvas-node system-canvas-node--link", style: { cursor: onPointerDown ? 'move' : 'pointer' }, onClick: (e) => onClick(node, e), onDoubleClick: (e) => onDoubleClick(node, e), onContextMenu: (e) => onContextMenu(node, e), onPointerDown: onPointerDown ? (e) => onPointerDown(node, e) : undefined, children: [_jsx("rect", { x: x, y: y, width: width, height: height, rx: node.resolvedCornerRadius, fill: theme.background }), _jsx("rect", { x: x, y: y, width: width, height: height, rx: node.resolvedCornerRadius, fill: node.resolvedFill, stroke: node.resolvedStroke, strokeWidth: theme.node.strokeWidth }), !isEditing && !slots?.body && (_jsx("text", { x: contentX + 12, y: contentY + contentHeight / 2 + 4, fill: node.resolvedStroke, fontSize: 12, fontFamily: theme.node.fontFamily, pointerEvents: "none", opacity: 0.6, children: '\u{29C9}' })), !isEditing && !slots?.body && (_jsx(NodeText, { region: {
                    x: contentX + glyphReserve,
                    y: contentY + LABEL_PAD_Y,
                    width: Math.max(0, contentWidth - glyphReserve - LABEL_PAD_X),
                    height: Math.max(0, contentHeight - LABEL_PAD_Y * 2),
                }, value: displayUrl, theme: theme, color: theme.node.labelColor, align: "center", fontWeight: 600, fontSize: theme.node.fontSize, wrap: true, verticalAlign: "center" })), slots && (_jsx(CategorySlotsLayer, { node: node, theme: theme, canvases: canvases, slots: slots })), node.isNavigable && (_jsx(RefIndicator, { node: node, theme: theme, nodeX: x, nodeY: y, nodeWidth: width, nodeHeight: height, strokeColor: node.resolvedStroke, strokeWidth: theme.node.strokeWidth, corner: toKebabCorner(refCorner), onNavigate: onNavigate }))] }));
}
//# sourceMappingURL=LinkNode.js.map