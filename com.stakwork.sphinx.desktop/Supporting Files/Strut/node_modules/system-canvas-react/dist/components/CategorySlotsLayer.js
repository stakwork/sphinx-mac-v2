import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useId, useMemo } from 'react';
import { computeCategorySlotRegions, getCategorySlots, resolveAccessor, resolveAccessorOr, rollupNodes, slotEntries, } from 'system-canvas';
import { NodeColorFill } from '../primitives/NodeColorFill.js';
import { NodeProgressBar } from '../primitives/NodeProgressBar.js';
import { NodeCountBadge } from '../primitives/NodeCountBadge.js';
import { NodeDot } from '../primitives/NodeDot.js';
import { NodeText } from '../primitives/NodeText.js';
import { NodeStatusPill } from '../primitives/NodeStatusPill.js';
import { NodeIcon } from './NodeIcon.js';
/**
 * Renders the category slots for a single node. Computes regions, runs
 * accessors, and dispatches to the appropriate primitive per `kind`.
 *
 * Emits nothing (no `<g>`) when the node's category has no slots. All slot
 * primitives set `pointerEvents="none"` internally so they never steal
 * clicks from the node body or the ref indicator.
 */
export function CategorySlotsLayer({ node, theme, canvases, slots: slotsProp, }) {
    const slots = slotsProp ?? getCategorySlots(node, theme);
    const regions = useMemo(() => computeCategorySlotRegions(node, theme, slots), [node, theme, slots]);
    // Stable, render-scoped id for the edge clipPath so multiple nodes on
    // the same canvas don't collide on `#clip-edge`.
    const reactId = useId();
    const clipId = `sc-edge-clip-${reactId.replace(/:/g, '')}`;
    if (!slots)
        return null;
    // Resolver used by slot context — pure lookup into the canvases map.
    const getSubCanvas = (ref) => canvases?.[ref];
    const entries = slotEntries(slots);
    if (entries.length === 0)
        return null;
    // Split into edge vs non-edge slots. Edge slots paint clipped to the
    // node's rounded rect so fills bleed under the corners without
    // overflowing. Non-edge slots paint unclipped.
    const edgeEntries = entries.filter(([p]) => isEdgePosition(p));
    const otherEntries = entries.filter(([p]) => !isEdgePosition(p));
    const renderEntry = ([position, spec]) => {
        const region = regions[position];
        const ctx = {
            node,
            theme,
            region,
            getSubCanvas,
            canvases,
            rollup: (predicate) => node.ref
                ? rollupNodes(getSubCanvas(node.ref), predicate)
                : { total: 0, matched: 0, fraction: 0 },
        };
        return _jsx(SlotView, { position: position, spec: spec, ctx: ctx }, position);
    };
    return (_jsxs("g", { className: "system-canvas-slots", pointerEvents: "none", children: [edgeEntries.length > 0 && (_jsxs(_Fragment, { children: [_jsx("defs", { children: _jsx("clipPath", { id: clipId, children: _jsx("rect", { x: node.x, y: node.y, width: node.width, height: node.height, rx: node.resolvedCornerRadius }) }) }), _jsx("g", { clipPath: `url(#${clipId})`, children: edgeEntries.map(renderEntry) })] })), otherEntries.map(renderEntry)] }));
}
function isEdgePosition(p) {
    return (p === 'topEdge' ||
        p === 'bottomEdge' ||
        p === 'leftEdge' ||
        p === 'rightEdge');
}
function SlotView({ position, spec, ctx, }) {
    try {
        return renderSlot(position, spec, ctx);
    }
    catch (err) {
        const env = globalThis.process?.env?.NODE_ENV;
        if (env !== 'production') {
            console.warn('[system-canvas] slot render failed', position, err);
        }
        return null;
    }
}
function renderSlot(position, spec, ctx) {
    const { theme, node, region } = ctx;
    // Universal color inheritance: when a slot omits `color`, it follows
    // the node's resolved stroke. This makes toolbar color changes
    // propagate naturally to every color-bearing slot (top strips, pills,
    // dots, badges, progress bars).
    const nodeColor = node.resolvedStroke;
    switch (spec.kind) {
        case 'color': {
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            const length = resolveAccessorOr(spec.length, 0.55, ctx);
            return (_jsx(NodeColorFill, { region: region, color: color, position: position, extent: spec.extent, length: length }));
        }
        case 'progress': {
            const value = resolveAccessor(spec.value, ctx);
            // `hideWhenZero` lets cards skip the empty track when there's no
            // signal yet (e.g. a milestone with zero linked features). The
            // empty track would otherwise read as "0% complete" rather than
            // "no data."
            if (spec.hideWhenZero && (!Number.isFinite(value) || value <= 0)) {
                return null;
            }
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            const bgColor = resolveAccessorOr(spec.bgColor, 'rgba(255,255,255,0.08)', ctx);
            return (_jsx(NodeProgressBar, { region: region, value: value, color: color, bgColor: bgColor }));
        }
        case 'count': {
            const raw = resolveAccessor(spec.value, ctx);
            const hideWhenEmpty = spec.hideWhenEmpty !== false;
            if (hideWhenEmpty) {
                if (raw === 0 || raw === '' || raw == null)
                    return null;
            }
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            const textColor = resolveAccessorOr(spec.textColor, theme.background, ctx);
            return (_jsx(NodeCountBadge, { region: region, value: raw, theme: theme, color: color, textColor: textColor, position: position }));
        }
        case 'pill': {
            const value = resolveAccessor(spec.value, ctx);
            if (!value)
                return null;
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            const textColor = spec.textColor
                ? resolveAccessor(spec.textColor, ctx)
                : undefined;
            const fill = spec.fill ? resolveAccessor(spec.fill, ctx) : undefined;
            return (_jsx(NodeStatusPill, { region: region, value: value, theme: theme, color: color, textColor: textColor, fill: fill }));
        }
        case 'text': {
            const value = resolveAccessor(spec.value, ctx);
            const color = resolveAccessorOr(spec.color, position === 'body' ? theme.node.labelColor : theme.node.sublabelColor, ctx);
            // Position-aware alignment default — headers/footers left-align,
            // body left-aligns (title-style), everything else centers.
            const defaultAlign = position === 'header' || position === 'footer' || position === 'body'
                ? 'start'
                : 'center';
            const align = spec.align ?? defaultAlign;
            // Headers read as kickers — uppercase, letter-spaced, bolder,
            // rendered in the label font. Body text reads as a title / figure —
            // label-font, bolder, not uppercase. Callers can override any of
            // these via explicit TextSlot fields.
            const isHeader = position === 'header';
            const isBody = position === 'body';
            const defaultWeight = isHeader ? 700 : isBody ? 600 : 500;
            const defaultUppercase = isHeader;
            const defaultUseLabelFont = isHeader || isBody;
            const defaultFontSize = isBody
                ? Math.round(theme.node.fontSize * 1.35)
                : undefined;
            const fontSize = spec.fontSize !== undefined
                ? resolveAccessor(spec.fontSize, ctx)
                : defaultFontSize;
            const fontWeight = spec.fontWeight !== undefined
                ? resolveAccessor(spec.fontWeight, ctx)
                : defaultWeight;
            const fontFamily = spec.fontFamily !== undefined
                ? resolveAccessor(spec.fontFamily, ctx)
                : undefined;
            // Wrapping defaults: `body` text wraps by default (title pattern);
            // every other position renders single-line. Honors `\n` either way.
            const wrap = spec.wrap ?? isBody;
            const lineHeight = spec.lineHeight !== undefined
                ? resolveAccessor(spec.lineHeight, ctx)
                : undefined;
            const fill = spec.fill !== undefined ? resolveAccessor(spec.fill, ctx) : undefined;
            return (_jsx(NodeText, { region: region, value: value, theme: theme, color: color, fill: fill, align: align, fontWeight: fontWeight, uppercase: spec.uppercase ?? defaultUppercase, useLabelFont: spec.useLabelFont ?? defaultUseLabelFont, fontFamily: fontFamily, fontSize: fontSize, wrap: wrap, maxLines: spec.maxLines, lineHeight: lineHeight }));
        }
        case 'dot': {
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            return _jsx(NodeDot, { region: region, color: color });
        }
        case 'icon': {
            const name = resolveAccessor(spec.name, ctx);
            if (!name)
                return null;
            const color = resolveAccessorOr(spec.color, nodeColor, ctx);
            const opacity = resolveAccessorOr(spec.opacity, 1, ctx);
            const mode = resolveAccessorOr(spec.mode, 'stroke', ctx);
            const viewBox = resolveAccessorOr(spec.viewBox, 16, ctx);
            // Default size = the shorter axis of the region minus a small inset,
            // so an icon in a 1.25em corner slot reads as a corner icon and an
            // icon in a larger `body` or `header` region scales up naturally.
            // Consumers can pin a fixed size via `spec.size`.
            const auto = Math.max(8, Math.min(region.width, region.height) - 2);
            const size = resolveAccessorOr(spec.size, auto, ctx);
            // Center the icon in the region. NodeIcon's transform origin is the
            // top-left of the icon's bounding square.
            const x = region.x + (region.width - size) / 2;
            const y = region.y + (region.height - size) / 2;
            return (_jsx(NodeIcon, { icon: name, x: x, y: y, size: size, color: color, opacity: opacity, customIcons: theme.icons, mode: mode, viewBox: viewBox }));
        }
        case 'custom': {
            return spec.render(ctx);
        }
    }
}
//# sourceMappingURL=CategorySlotsLayer.js.map