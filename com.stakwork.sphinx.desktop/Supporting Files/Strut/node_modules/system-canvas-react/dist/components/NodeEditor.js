import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useEffect, useMemo, useRef, useState } from 'react';
import { computeReflowReservations, getAtPath, getCategorySlots, setAtPath, } from 'system-canvas';
/**
 * Inline editor rendered inside a <foreignObject> overlaying the node.
 *
 * Two modes:
 *   1. **Form editor** — when the node's category declares `editableFields`,
 *      render one control per field (text / textarea / number / select /
 *      boolean). Commit produces a single merged patch.
 *   2. **Single-field editor** — the historical default:
 *        - text  → <textarea>
 *        - file  → <input>
 *        - link  → <input>
 *        - group → <input>
 *
 * Commit semantics:
 *   - Blur of the whole panel (focus leaves all fields) → commit
 *   - Escape                                            → cancel
 *   - Enter on a non-textarea field                     → commit (single
 *     field) or advance focus to next field (form); Enter on last field
 *     commits.
 *   - Cmd/Ctrl+Enter inside a textarea                  → commit
 */
export function NodeEditor({ node, theme, onCommit, onCancel }) {
    const editableFields = useCategoryFields(node, theme);
    if (editableFields) {
        return (_jsx(FormEditor, { node: node, theme: theme, fields: editableFields, onCommit: onCommit, onCancel: onCancel }));
    }
    return (_jsx(SingleFieldEditor, { node: node, theme: theme, onCommit: onCommit, onCancel: onCancel }));
}
function useCategoryFields(node, theme) {
    if (!node.category)
        return null;
    const def = theme.categories[node.category];
    const fields = def?.editableFields;
    if (!fields || fields.length === 0)
        return null;
    return fields;
}
// ---------------------------------------------------------------------------
// Single-field editor (historical default)
// ---------------------------------------------------------------------------
function SingleFieldEditor({ node, theme, onCommit, onCancel, }) {
    const initial = getInitialValue(node);
    const [value, setValue] = useState(initial);
    const textareaRef = useRef(null);
    const inputRef = useRef(null);
    const committedRef = useRef(false);
    useEffect(() => {
        const el = textareaRef.current ?? inputRef.current;
        if (el) {
            el.focus();
            const end = el.value.length;
            el.setSelectionRange(end, end);
        }
    }, []);
    const commit = () => {
        if (committedRef.current)
            return;
        committedRef.current = true;
        if (value === initial) {
            onCancel();
            return;
        }
        onCommit(buildSingleFieldPatch(node, value));
    };
    const cancel = () => {
        if (committedRef.current)
            return;
        committedRef.current = true;
        onCancel();
    };
    const stopPointer = (e) => {
        e.stopPropagation();
    };
    const fontFamily = theme.node.fontFamily;
    const fontSize = theme.node.fontSize;
    const padding = 8;
    // Mirror TextNode's display-alignment rule so the editor matches the
    // committed appearance: when the category declares a header slot
    // (i.e. `reservedTop > 0`), the node is a dashboard-style card whose
    // label is left-aligned under the header. Otherwise plain `type: 'text'`
    // nodes center (historical behavior); file/link/group always left-align.
    const slots = getCategorySlots(node, theme);
    const reservations = computeReflowReservations(node, theme, slots);
    const hasHeader = reservations.top > 0;
    const textAlign = node.type === 'text' && !hasHeader ? 'center' : 'left';
    const commonFieldStyle = {
        width: '100%',
        height: '100%',
        boxSizing: 'border-box',
        padding,
        fontFamily,
        fontSize,
        background: theme.background,
        color: theme.node.labelColor,
        border: `1.5px solid ${theme.node.labelColor}`,
        borderRadius: node.resolvedCornerRadius,
        outline: 'none',
        resize: 'none',
        textAlign,
    };
    return (_jsx("foreignObject", { x: node.x, y: node.y, width: node.width, height: node.height, onPointerDown: stopPointer, onMouseDown: stopPointer, onClick: stopPointer, onDoubleClick: stopPointer, children: node.type === 'text' ? (_jsx("textarea", { ref: textareaRef, value: value, onChange: (e) => setValue(e.target.value), onBlur: commit, onKeyDown: (e) => {
                if (e.key === 'Enter' && !e.shiftKey) {
                    e.preventDefault();
                    commit();
                }
                else if (e.key === 'Escape') {
                    e.preventDefault();
                    cancel();
                }
            }, style: commonFieldStyle })) : (_jsx("input", { ref: inputRef, value: value, onChange: (e) => setValue(e.target.value), onBlur: commit, onKeyDown: (e) => {
                if (e.key === 'Enter') {
                    e.preventDefault();
                    commit();
                }
                else if (e.key === 'Escape') {
                    e.preventDefault();
                    cancel();
                }
            }, style: commonFieldStyle })) }));
}
function getInitialValue(node) {
    switch (node.type) {
        case 'text':
            return node.text ?? '';
        case 'file':
            return node.file ?? '';
        case 'link':
            return node.url ?? '';
        case 'group':
            return node.label ?? '';
        default:
            return '';
    }
}
function buildSingleFieldPatch(node, value) {
    switch (node.type) {
        case 'text':
            return { text: value };
        case 'file':
            return { file: value };
        case 'link':
            return { url: value };
        case 'group':
            return { label: value };
        default:
            return {};
    }
}
function FormEditor({ node, theme, fields, onCommit, onCancel, }) {
    // Initial values read from the node at mount; driven by local state.
    const initial = useMemo(() => readInitialValues(node, fields), [node, fields]);
    const [values, setValues] = useState(initial);
    const committedRef = useRef(false);
    const panelRef = useRef(null);
    // The panel grows with the fields list. We size it at least as wide as
    // the node and let the container overflow below the node rect.
    const width = Math.max(node.width, 240);
    // 1-based rough height estimate — lets <foreignObject> give enough room.
    const height = Math.max(node.height, 36 + fields.length * 44);
    const commit = () => {
        if (committedRef.current)
            return;
        committedRef.current = true;
        const patch = buildFormPatch(node, fields, initial, values);
        if (!patch) {
            onCancel();
            return;
        }
        onCommit(patch);
    };
    const cancel = () => {
        if (committedRef.current)
            return;
        committedRef.current = true;
        onCancel();
    };
    // Commit on blur of the *panel* — fires when focus leaves all fields.
    const onBlurPanel = (e) => {
        const next = e.relatedTarget;
        if (next && panelRef.current?.contains(next))
            return;
        commit();
    };
    const stopPointer = (e) => {
        e.stopPropagation();
    };
    // Focus the first focusable control on mount.
    useEffect(() => {
        const el = panelRef.current;
        if (!el)
            return;
        const first = el.querySelector('input, textarea, select, button');
        first?.focus();
        if (first instanceof HTMLInputElement && first.type === 'text') {
            const end = first.value.length;
            first.setSelectionRange(end, end);
        }
    }, []);
    const fieldLabelColor = theme.node.sublabelColor;
    const inputStyle = {
        width: '100%',
        boxSizing: 'border-box',
        padding: '6px 8px',
        fontFamily: theme.node.fontFamily,
        fontSize: theme.node.fontSize - 1,
        background: theme.background,
        color: theme.node.labelColor,
        border: `1px solid ${theme.breadcrumbs.separatorColor}`,
        borderRadius: 6,
        outline: 'none',
    };
    return (_jsx("foreignObject", { x: node.x, y: node.y, width: width, height: height, onPointerDown: stopPointer, onMouseDown: stopPointer, onClick: stopPointer, onDoubleClick: stopPointer, children: _jsx("div", { ref: panelRef, onBlur: onBlurPanel, onKeyDown: (e) => {
                if (e.key === 'Escape') {
                    e.preventDefault();
                    cancel();
                }
                else if (e.key === 'Enter' &&
                    (e.metaKey || e.ctrlKey)) {
                    e.preventDefault();
                    commit();
                }
                else if (e.key === 'Enter' &&
                    !(e.target instanceof HTMLTextAreaElement)) {
                    // Advance focus to the next field; commit on last.
                    e.preventDefault();
                    const panel = panelRef.current;
                    if (!panel)
                        return;
                    const focusables = Array.from(panel.querySelectorAll('input, textarea, select'));
                    const idx = focusables.indexOf(e.target);
                    if (idx === -1 || idx === focusables.length - 1)
                        commit();
                    else
                        focusables[idx + 1].focus();
                }
            }, style: {
                width: '100%',
                boxSizing: 'border-box',
                display: 'flex',
                flexDirection: 'column',
                gap: 8,
                padding: 10,
                background: theme.breadcrumbs.background,
                color: theme.node.labelColor,
                border: `1.5px solid ${theme.node.labelColor}`,
                borderRadius: node.resolvedCornerRadius,
                boxShadow: '0 4px 12px rgba(0,0,0,0.25)',
                backdropFilter: 'blur(8px)',
                fontFamily: theme.node.fontFamily,
            }, children: fields.map((field) => {
                const v = values[field.path];
                const setV = (next) => setValues((prev) => ({ ...prev, [field.path]: next }));
                const label = field.label ?? field.path;
                return (_jsxs("label", { style: {
                        display: 'flex',
                        flexDirection: 'column',
                        gap: 3,
                        fontSize: 10,
                        color: fieldLabelColor,
                        textTransform: 'uppercase',
                        letterSpacing: 0.5,
                    }, children: [_jsx("span", { children: label }), renderControl(field, v, setV, inputStyle, theme)] }, field.path));
            }) }) }));
}
function renderControl(field, value, setValue, inputStyle, theme) {
    switch (field.kind) {
        case 'textarea':
            return (_jsx("textarea", { value: value ?? '', onChange: (e) => setValue(e.target.value), placeholder: field.placeholder, rows: 3, style: { ...inputStyle, resize: 'vertical', minHeight: 60 } }));
        case 'number':
            return (_jsx("input", { type: "number", value: value == null ? '' : String(value), min: field.min, max: field.max, step: field.step, onChange: (e) => {
                    const v = e.target.value;
                    setValue(v === '' ? undefined : Number(v));
                }, placeholder: field.placeholder, style: inputStyle }));
        case 'select':
            return (_jsxs("select", { value: value ?? '', onChange: (e) => setValue(e.target.value), style: inputStyle, children: [!(field.options?.some((o) => o.value === value)) && (_jsx("option", { value: "", children: "\u2014" })), field.options?.map((o) => (_jsx("option", { value: o.value, children: o.label ?? o.value }, o.value)))] }));
        case 'boolean':
            return (_jsx("input", { type: "checkbox", checked: !!value, onChange: (e) => setValue(e.target.checked), style: {
                    alignSelf: 'flex-start',
                    width: 16,
                    height: 16,
                    accentColor: theme.node.labelColor,
                } }));
        case 'text':
        default:
            return (_jsx("input", { type: "text", value: value ?? '', onChange: (e) => setValue(e.target.value), placeholder: field.placeholder, style: inputStyle }));
    }
}
function readInitialValues(node, fields) {
    const out = {};
    for (const f of fields) {
        out[f.path] = getAtPath(node, f.path);
    }
    return out;
}
/**
 * Build the commit patch. Each field's dot-path is written into a pseudo
 * node via `setAtPath`, starting from the current node (so untouched
 * `customData` siblings are preserved). The top-level keys on the result
 * become the patch.
 *
 * Returns null when nothing actually changed.
 */
function buildFormPatch(node, fields, initial, current) {
    let changed = false;
    for (const f of fields) {
        if (current[f.path] !== initial[f.path]) {
            changed = true;
            break;
        }
    }
    if (!changed)
        return null;
    // Work off a shallow clone of the node so nested writes don't mutate.
    let working = { ...node };
    for (const f of fields) {
        working = setAtPath(working, f.path, current[f.path]);
    }
    // Build the patch by collecting the top-level keys that any field writes
    // into. For `customData.foo` the top-level key is `customData` and we
    // emit the whole merged `customData` object.
    const patch = {};
    const topKeys = new Set();
    for (const f of fields) {
        topKeys.add(f.path.split('.')[0]);
    }
    for (const k of topKeys) {
        ;
        patch[k] = working[k];
    }
    return patch;
}
//# sourceMappingURL=NodeEditor.js.map